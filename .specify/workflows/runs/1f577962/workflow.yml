schema_version: '1.0'
workflow:
  id: speckit
  name: Full SDD Cycle
  version: 1.0.0
  author: GitHub
  description: "Runs specify \u2192 plan \u2192 tasks \u2192 implement with review\
    \ gates"
requires:
  speckit_version: '>=0.8.5'
  integrations:
    any:
    - alquimia
    - claude
    - copilot
    - gemini
    - opencode
inputs:
  spec:
    type: string
    required: true
    prompt: Describe what you want to build
  integration:
    type: string
    default: auto
    prompt: Integration to use (e.g. claude, copilot, gemini; 'auto' uses the project's
      initialized integration)
  scope:
    type: string
    default: full
    enum:
    - full
    - backend-only
    - frontend-only
steps:
- id: specify
  command: speckit.specify
  integration: '{{ inputs.integration }}'
  input:
    args: '{{ inputs.spec }}'
- id: review-spec
  type: gate
  message: Review the generated spec before planning.
  options:
  - approve
  - reject
  on_reject: abort
- id: plan
  command: speckit.plan
  integration: '{{ inputs.integration }}'
  input:
    args: '{{ inputs.spec }}'
- id: review-plan
  type: gate
  message: Review the plan before generating tasks.
  options:
  - approve
  - reject
  on_reject: abort
- id: tasks
  command: speckit.tasks
  integration: '{{ inputs.integration }}'
  input:
    args: '{{ inputs.spec }}'
- id: implement
  command: speckit.implement
  integration: '{{ inputs.integration }}'
  input:
    args: '{{ inputs.spec }}'
- id: approve-commit
  type: gate
  message: "Confira a implementa\xE7\xE3o e aprove o commit."
  options:
  - approve
  - reject
  on_reject: abort
- id: commit
  type: shell
  run: 'git add .

    git commit

    '
